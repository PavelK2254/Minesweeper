import { TileCleanerPipe } from './tile-cleaner.pipe';

describe('TileCleanerPipe', () => {
  it('create an instance', () => {
    const pipe = new TileCleanerPipe();
    expect(pipe).toBeTruthy();
  });
});
